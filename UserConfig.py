import Controler


class UserConfig:
    def __init__(self):
        self.profile_name = 'sample_profile'
        self.username = 'sample-username'
        self.password = 'sample-password'
        self.driver_path = None  # default is in the same file
